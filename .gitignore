# Build artifacts
build/
install/
log/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
graphify-out/

# Internal machine artifact for the param-coverage backlog (BACKLOG.md is the
# committed human-readable roadmap; backlog.json is regenerated each run).
backlog.json
