# nav2_config parameter coverage backlog

Auto-generated by `nav2_param_gap.py`. The canonical Nav2 example config (`nav2_bringup/params/nav2_params.yaml`) declares **262** node/param pairs. The schema currently covers **261** of them; **1** are not yet in the schema (total schema size: 648, incl. params from other source configs).

Each daily commit should add accurate schema entries (real default/type/range from
the cited source, plus a genuine description + tuning `impact`) for one node's params.

## docking_server (1)
- [ ] `simulation_step` (default `0.1`, double)

## Ideas for review

- The one remaining backlog item, `docking_server` / `simulation_step`, is a **false positive**:
  the source (`nav2_docking/opennav_docking/src/controller.cpp:61-62`) declares the parameter as
  `controller.simulation_time_step` (already covered by the schema). The name `simulation_step` only
  appears in `nav2_bringup/params/nav2_params.yaml:585` — a stale param name in the upstream example
  config that is never declared/read by the docking server. Worth reporting upstream to Nav2.
- `following_server` still has 2 uncovered source-declared params for a future day:
  `controller.use_collision_detection` (default false — note it differs from docking_server's true)
  and `filter_coef` (see `opennav_following/src/parameter_handler.cpp`).
