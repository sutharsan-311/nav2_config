# nav2_config parameter coverage backlog

Auto-generated by `nav2_param_gap.py`. The canonical Nav2 example config (`nav2_bringup/params/nav2_params.yaml`) declares **262** node/param pairs. The schema currently covers **261** of them; **1** are not yet in the schema (total schema size: 623, incl. params from other source configs).

Each daily commit should add accurate schema entries (real default/type/range from
the cited source, plus a genuine description + tuning `impact`) for one node's params.

## docking_server (1)
- [ ] `simulation_step` (default `0.1`, double)
