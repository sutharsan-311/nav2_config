# nav2_config parameter coverage backlog

Auto-generated by `nav2_param_gap.py`. The canonical Nav2 example config (`nav2_bringup/params/nav2_params.yaml`) declares **262** node/param pairs. The schema currently covers **261** of them; **1** are not yet in the schema (total schema size: 632, incl. params from other source configs).

Each daily commit should add accurate schema entries (real default/type/range from
the cited source, plus a genuine description + tuning `impact`) for one node's params.

## docking_server (1)
- [ ] `simulation_step` (default `0.1`, double)

## Ideas for review
- Upstream discrepancy: `nav2_bringup/params/nav2_params.yaml` (line ~585) uses the key
  `controller.simulation_step` for docking_server, but the actual declaration in
  `nav2_docking/opennav_docking/src/controller.cpp` is `controller.simulation_time_step`
  (already covered by the schema). The yaml key appears stale — worth an upstream
  navigation2 PR/issue, and the gap script reports it as a false-positive missing param.
