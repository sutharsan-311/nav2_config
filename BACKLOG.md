# nav2_config parameter coverage backlog

Auto-generated by `nav2_param_gap.py`. The canonical Nav2 example config (`nav2_bringup/params/nav2_params.yaml`) declares **262** node/param pairs. The schema currently covers **261** of them; **1** are not yet in the schema (total schema size: 640, incl. params from other source configs).

Each daily commit should add accurate schema entries (real default/type/range from
the cited source, plus a genuine description + tuning `impact`) for one node's params.

## docking_server (1)
- [ ] `simulation_step` (default `0.1`, double)

## Ideas for review
- `docking_server / simulation_step` backlog item is a FALSE POSITIVE: `nav2_bringup/params/nav2_params.yaml:585` uses the stale key `controller.simulation_step`, but the source only declares `controller.simulation_time_step` (opennav_docking/src/controller.cpp:61-62), which the schema already covers. Consider an exclusion list in `nav2_param_gap.py` (and optionally an upstream Nav2 PR fixing the yaml).
- The new `following_server` node (opennav_following) is in the schema but not in `NAV2_NODE_SPECS` in `nav2_config/core/node_discovery.py`, so the GUI won't discover a live following_server yet (same situation as docking_server). Adding it is a small runtime change — needs Sutharsan's review.
