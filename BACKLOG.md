# nav2_config parameter coverage backlog

Auto-generated by `nav2_param_gap.py`. The canonical Nav2 example config (`nav2_bringup/params/nav2_params.yaml`) declares **262** node/param pairs. The schema currently covers **261** of them; **1** are not yet in the schema (total schema size: 602, incl. params from other source configs).

Each daily commit should add accurate schema entries (real default/type/range from
the cited source, plus a genuine description + tuning `impact`) for one node's params.

## docking_server (1)
- [ ] `simulation_step` (default `0.1`, double)

## Ideas for review
- **Stale param name in nav2_bringup YAML (upstream Nav2 issue, not ours):** `nav2_bringup/params/nav2_params.yaml` sets `docking_server: controller.simulation_step`, but the code (`nav2_docking/opennav_docking/src/controller.cpp:61-62`) declares `controller.simulation_time_step` — the YAML key is silently ignored by the docking server. Our schema already covers the real param (`simulation_time_step`). The gap script flags this as a false-positive missing param; consider teaching it an alias/ignore list, and possibly filing an upstream Nav2 PR to fix the YAML.
- **Near-duplicate schema categories:** the `category` field contains overlapping pairs — `debug`/`debugging`, `filter`/`filtering`, `sensor`/`sensors`, `timeouts`/`timing`, `tolerances`/`goal_tolerance`. Consolidating them would make GUI category filtering cleaner. Needs a decision on canonical names before touching data.
