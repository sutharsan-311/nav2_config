# nav2_config parameter coverage backlog

Auto-generated by `nav2_param_gap.py`. The canonical Nav2 example config (`nav2_bringup/params/nav2_params.yaml`) declares **262** node/param pairs. The schema currently covers **261** of them; **1** are not yet in the schema (total schema size: 617, incl. params from other source configs).

Each daily commit should add accurate schema entries (real default/type/range from
the cited source, plus a genuine description + tuning `impact`) for one node's params.

## docking_server (1)
- [ ] `simulation_step` (default `0.1`, double)

## Ideas for review

- The single remaining backlog item, `docking_server` / `controller.simulation_step`, appears to be a stale name in upstream `nav2_bringup/params/nav2_params.yaml`: the actual parameter declared in `nav2_docking/opennav_docking/src/controller.cpp` (L61-62) is `controller.simulation_time_step`, which the schema already covers. Adding `simulation_step` would document a parameter that does not exist. Suggest reporting the yaml typo upstream to ros-navigation/navigation2 rather than adding it to the schema.
