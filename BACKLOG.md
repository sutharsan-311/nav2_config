# nav2_config parameter coverage backlog

Auto-generated by `nav2_param_gap.py`. The canonical Nav2 example config (`nav2_bringup/params/nav2_params.yaml`) declares **262** node/param pairs. The schema currently covers **261** of them; **1** are not yet in the schema (total schema size: 630, incl. params from other source configs).

Each daily commit should add accurate schema entries (real default/type/range from
the cited source, plus a genuine description + tuning `impact`) for one node's params.

## docking_server (1)
- [ ] `simulation_step` (default `0.1`, double)

## Ideas for review
- The remaining `docking_server: simulation_step` backlog item is a false positive: the actual declared parameter is `controller.simulation_time_step` (navigation2 `nav2_docking/opennav_docking/src/controller.cpp:61-62`), which the schema already covers. `simulation_step` is a stale name in upstream `nav2_bringup/params/nav2_params.yaml:585`. Consider adding an ignore/alias list to `nav2_param_gap.py` (and possibly reporting the yaml typo upstream).
- `nav2_param_gap.py` only diffs against the bringup yaml, so nodes like `route_server` have real coverage gaps it can't see (yaml shows only a subset of declared params). Could extend the script to scan `declare_or_get_parameter` calls in navigation2 source per node.
